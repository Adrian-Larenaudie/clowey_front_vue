import { createStore } from 'vuex';
import axios from 'axios';

const admin = createStore({
    state:() => ({
        connected: false,
    }), 
    
    getters: {

    },

    actions: {
        
    },
    
    mutations: {
      
    },
});

export default admin;